#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - main block
 * Return: 0
 */
int main(void)
{
	char low;

	for (low = 'a'; low <= 'z'; loww++)
		putchar(low);
	putchar("\n");
	return (0);
}
