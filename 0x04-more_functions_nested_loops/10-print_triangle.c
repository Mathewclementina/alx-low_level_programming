#include "main.h"

/**
 * print_triangle -> print a triangle
 * @size: size of the shape
 * Return: o
 */
void print_triangle(int size)
{

	int l, b;

	if (size <= 0)
	{
		_putchar('\n');
	}
	els
		for (l = 0; l <= (size - 1); l++)
		{
			for (b = 0; b < (size - 1); b++)
				_putchar('#');
		}
	if (l == size)
	{
		continue;
	}
	_putchar('\n');
}

